# Endpoint the hero composer POSTs enquiries to as {"prompt": "..."}.
# Must be reachable from the browser and CORS-enabled: the site is deployed to
# Amplify as static files (see amplify.yml), so there is no server at runtime.
VITE_ENQUIRY_ENDPOINT=
